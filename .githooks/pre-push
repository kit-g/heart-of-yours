#!/bin/sh
# Runs the same gate CI runs — analysis + format check (see unit-tests.yml).
# Installed by `make bootstrap` via core.hooksPath.
# Skip in an emergency with `git push --no-verify`.
set -e
cd "$(git rev-parse --show-toplevel)"
make lint
