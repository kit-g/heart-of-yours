name: Unit Tests

on:
  push:
    branches: [ main ]
  pull_request: { }
  workflow_dispatch: { }
  workflow_call: { } # allows other workflows to call this one

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Every job runs the same make targets developers run locally — the recipe
# lives in the Makefile, not here.
jobs:
  lint:
    name: "Analyze"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Flutter
        uses: flutter-actions/setup-flutter@921d50dce8cdd6aaf578d12f91c013095de220e5 # v4.3
        with:
          channel: 'stable'
          version: '3.44.8' # keep in sync with the other workflows and the README
          cache: true
          cache-sdk: true

      - name: Dependencies
        run: make deps

      - name: Analyze
        run: make lint

  test_packages:
    name: "Pkg • ${{ matrix.package }}"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        package:
          - heart_api
          - heart_db
          - heart_state
          - heart_charts
          - heart_language

    steps:
      - uses: actions/checkout@v6

      - name: Set up Flutter
        uses: flutter-actions/setup-flutter@921d50dce8cdd6aaf578d12f91c013095de220e5 # v4.3
        with:
          channel: 'stable'
          version: '3.44.8' # keep in sync with the other workflows and the README
          cache: true
          cache-sdk: true

      - name: Dependencies
        run: make deps

      - name: "Run tests"
        run: make test-${{ matrix.package }}

  test_app:
    name: "App • tests"
    runs-on: ubuntu-latest
    needs: [ test_packages ]
    steps:
      - uses: actions/checkout@v6

      - name: Set up Flutter
        uses: flutter-actions/setup-flutter@921d50dce8cdd6aaf578d12f91c013095de220e5 # v4.3
        with:
          channel: 'stable'
          version: '3.44.8' # keep in sync with the other workflows and the README
          cache: true
          cache-sdk: true

      - name: Dependencies
        run: make deps

      - name: Run app tests
        run: make test-app
