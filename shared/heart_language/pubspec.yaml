name: heart_language
description: "Language resource"
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  # kept in lockstep with flutter_localizations' pin
  intl: ^0.20.2

resolution: workspace

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  args: ^2.4.2
  csv: ^8.0.0

flutter:
  generate: true