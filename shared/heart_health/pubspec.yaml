name: heart_health
description: "Read-only access to the device's health store (HealthKit / Health Connect)"
version: 0.1.0
homepage: 'https://github.com/kit-g/heart-of-yours'
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  health: ^13.3.2
  logging: ^1.3.0
  url_launcher: ^6.3.2

resolution: workspace

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0
  mockito: ^5.8.1
  build_runner: ^2.4.6
