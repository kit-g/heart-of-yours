# Store listings

Everything the two consoles ask for, written once, here, so the App Store and
Play copies say the same thing and both agree with the health-data contract
(`docs/2026-09-05.health-data.md`) and the public policy (`site/privacy.html`
in heart-api).

Nothing in this file is uploaded by CI. `google-play-deployment.yml` pushes
only `release_notes/whatsnew/`. The **App Store** text below was written
straight to App Store Connect through the API on 2026-09-08 (see *What is
already written*); the **Play** text is still pasted by hand. Character
counts in parentheses are the store's limit; the drafts are within them.

## What is already written

App Store Connect, app `me.heart-of.ios`, id **6777837444** (the dev record
is 6777837514 and is not touched). The listing was empty apart from the app
name; everything below now reads back from the API:

| Field | Value |
|---|---|
| Subtitle | `Every beat counts` — provisional, see below |
| Privacy Policy URL | `https://heart-of.me/privacy.html` |
| Description | the 1923-character text below |
| Keywords | the 94-character list below |
| Promotional text | the 166-character text below |
| Support URL | `https://heart-of.me/support.html` |
| Marketing URL | `https://heart-of.me` |
| Primary / secondary category | Health & Fitness / Sports |
| iOS version record | `1.0` → **`1.7.0`**, so a 1.7.0 build can be attached |

Left alone deliberately: the second version record for **macOS 1.0**, because
the Mac app is not ready; *What's New*, which the first release does not use;
and the age-rating questionnaire, which the API can set but which is worth
answering deliberately.

How it was written, for the next time: mint an ES256 JWT from the App Store
Connect key (`~/.appstoreconnect/private_keys/AuthKey_CKGD3LH3ZH.p8`, issuer
in `secrets/appstore/appstore_key.json` in S3) and `PATCH`
`/v1/appInfoLocalizations/{id}`, `/v1/appStoreVersionLocalizations/{id}`,
`/v1/appInfos/{id}` and `/v1/appStoreVersions/{id}`. Note that reading an
`appInfo` back reports `primaryCategory` as null unless you ask for
`?include=primaryCategory` — the write is fine, the read lies.

Facts the copy rests on: version 1.7.0, app name *Heart of yours*, bundle
`me.heart` / `me.heart.android`, domain `heart-of.me`, free, no ads, no
purchases, English + Spanish + French + Russian (and `en-CA`).

## App Store Connect

### App Name (30)

**Heart of yours** (14)

The alternative, if the plain name is too quiet in search:
*Heart of yours: Workout Log* (27). Apple indexes the name, so the second
buys the two words that matter most; it also spends the brand's calm.
Recommendation: keep the plain name, put the keywords in the subtitle.

### Subtitle (30)

**Currently set: `Every beat counts`** (17) — the README's own line, chosen
as a placeholder to revisit.

The drafted alternatives, if the tagline turns out to be too quiet for
search. Apple indexes the subtitle, and *workout* / *log* / *account* are
the words people type. Recommended of the three:

**Train. Log. No account needed.** (30)

Also exactly 30:
- *A workout log that stays yours*
- *Lift, log, and keep it private*

### Promotional text (170)

Editable without a build — use it for whatever the current version leads with.

> No sign-up, no email, no waiting. Open Heart and log your first set;
> your workouts stay on your phone until you decide otherwise. Log in later
> and they come with you.

### Description (4000)

> **A workout log that gets out of your way.**
>
> Plan a session, tick off every set, watch your lifts grow. Heart is a
> minimalist gym tracker: no feed, no streak guilt, no upsell in the middle
> of a working set.
>
> **Start without an account**
> Open the app and train. Your workouts, templates, custom exercises and
> goals live on your phone from the first session. No sign-up, no email.
> Log in whenever you like and everything you logged first comes with you —
> nothing is left behind.
>
> **Log a session**
> • A rest timer between sets, on the workout screen where you need it
> • Weight and reps per set, in kilograms or pounds
> • Save any session as a template, and file your templates in folders
> • Your own exercises, alongside a library you can search by muscle group
>
> **See it add up**
> • Personal records at every rep count, and lifetime totals
> • Charts per exercise and per week, on a dashboard that is yours to arrange
> • Goals you set yourself, with the rungs on the way to them
>
> **Health, on your terms**
> Heart shows your resting heart rate, heart-rate variability, sleep, steps,
> active energy and weight from Apple Health alongside your training, so you
> can see how they move together — and it saves each finished workout back to
> Health so your rings credit your lifting.
>
> Health data never leaves your device. Not the readings, not averages of
> them, not in a crash report, not in an export. There is no server table for
> it, because there is no server call for it.
>
> **Your data is yours**
> • Export everything as JSON or CSV, any time, with or without an account
> • Erase everything on the phone, in one place in Settings
> • Bring your history with you: import a CSV from another tracker
>
> **Built for the device you own**
> iPhone and iPad, in both orientations. On iPad, History and Exercises open
> two panes and the tab bar becomes a rail — not a phone layout stretched wide.
>
> Free. No ads. Nothing to subscribe to.
>
> English, Spanish, French and Russian.

### Keywords (100, comma-separated, no spaces)

Do not repeat the app name or the subtitle — Apple indexes those already.

> gym,lifting,strength,tracker,sets,reps,barbell,exercise,routine,fitness,
> offline,private,health

(One line in the console, no spaces after the commas: 94 characters.)

### URLs

| Field | Value |
|---|---|
| Support URL | `https://heart-of.me/support.html` — live, with `info@heart-of.me` as a mailto and links out to privacy, terms and account deletion |
| Marketing URL | `https://heart-of.me` |
| Privacy Policy URL | `https://heart-of.me/privacy.html` — verified 200; `/privacy` without the extension 403s |

### What's New

`release_notes/testflight.txt` for the TestFlight build; the store's
*What's New* takes the shorter public list — the same one Play gets
(`release_notes/whatsnew/whatsnew-en-US`), which the `release-notes` skill
writes.

## Google Play

### Title (30)

**Heart of yours: Workout Log** (27)

Play's search leans harder on the title than Apple's does, and Play has no
subtitle to carry the words instead.

### Short description (80)

Recommended:

**Log your lifts without an account. Your workouts stay on your phone.** (68)

Alternatives:
- *A minimalist gym tracker. No sign-up, no ads, your data on your device.* (71)
- *Track every set, rest, and record. Works offline, with no account.* (66)

### Full description (4000)

The App Store description above works nearly as-is; three changes:

1. Apple Health → **Health Connect** in the health section.
2. iPhone/iPad → **phones and tablets**; keep the two-pane sentence.
3. Play indexes the full description, so keep the words *workout tracker*,
   *gym*, *strength training* and *exercise log* in the opening paragraph.

Opening paragraph, Play version:

> **A workout tracker that gets out of your way.** Heart is a minimalist gym
> and strength-training log: plan a session, tick off every set, watch your
> lifts grow. No feed, no streak guilt, no upsell in the middle of a working
> set.

### What's new (500)

`release_notes/whatsnew/whatsnew-en-US`, already written and already uploaded
by the deploy workflow.

## Play Console — what it takes

Package `me.heart.android` (dev is `me.heart.android.dev`). Note it does not
match the iOS bundle `me.heart-of.ios`; nothing to fix, but it surprises people
looking for one identifier.

**The listing text was written through the API** on 2026-09-09, with the same
service account CI uses (`github-actions@heart-of-yours`, key at
`secrets/playstore/playstore-sa-key.json` in S3): `edits.insert` →
`edits.listings.update` → `edits.commit`. Graphics go the same way through
`edits.images.upload`.

**`edits.commit` is the permission gate, not `insert`.** With release-only
permissions, `insert`, `listings.update` and `delete` all return 200 and the
commit 403s — so a probe that stops short of committing proves nothing. What
unblocked it was granting **"Edit and delete draft apps"** in Play Console →
Users and permissions. Note the scope of that permission: it covers apps with
no production release. Once Heart is published, editing the live listing is
likely to need **"Manage store presence"** instead, and this will start 403ing
again at the commit.

Everything under *App content* is a console form with no API at all.

### Listing (API-writable)

| Field | State |
|---|---|
| App name (30) | **written** — `Heart of yours: Workout Log` |
| Short description (80) | **written** — 68 chars |
| Full description (4000) | **written** — 1931 chars |
| Phone screenshots, 2–8 | **missing** |
| 7" and 10" tablet screenshots | **missing** — without them Play flags the app as not optimised for tablets, which is false and costs featuring |
| Feature graphic 1024×500 | **missing**, and required |
| App icon 512×512 | from the launcher icon |

### App content (console only)

Privacy policy URL · Ads declaration (no ads) · App access · Content rating
(IARC questionnaire) · Target audience and content · Data safety · Government
apps · Financial features · **Health apps** · News · Data deletion.

Answers that are already settled: no ads, no financial features, not a news or
government app, not targeting children. Data safety is drafted above. Data
deletion points at `heart-of.me/account.html`, which already exists. **App
access** needs Sofia's credentials, with the note that the app is fully usable
without an account and the login only exercises sync.

### The three that bite

1. **Health apps declaration.** Lists and justifies every Health Connect
   permission one by one, and Google asks for a **video** of the permission
   flow showing each granted permission in use. Reviewed in weeks. This is the
   long pole of the entire Play submission — start it before anything else.
2. **Testing requirement.** A *personal* Play developer account opened after
   Nov 2023 must run a closed test with 12 testers for 14 consecutive days
   before it can apply for production access. An organisation account is
   exempt. If this account is personal, that clock is the release date, and
   nothing else on this page matters until it starts.
3. **Advertising ID.** Firebase Analytics generally merges
   `com.google.android.gms.permission.AD_ID` into the manifest even though the
   app declares no ads, and Play requires the advertising-ID declaration to
   match. It is not in the source manifests; confirm from a *merged* manifest
   after an Android build, then either declare the use or add a
   `tools:node="remove"` for the permission. Guessing either way risks a policy
   rejection.

## Apple privacy labels

**Console only.** There is no App Store Connect API for this: `appDataUsages`,
`appDataUsageCategories` and every publish-state path 404 as non-existent
resources, and none of the 41 relationships on `/v1/apps/{id}` is privacy
related. App Store Connect → App Privacy, in the sidebar under General. It is
app-level rather than per-version and has its own **Publish** button, so it
can sit filled in but unpublished.

Apple offers exactly six purposes — Third-Party Advertising; Developer's
Advertising or Marketing; Analytics; Product Personalization; App
Functionality; Other Purposes. There is no "customer support" purpose; the
bug-report screenshot and text are App Functionality.

Apple's "collect" means *leaves the device*. An account-free install
transmits nothing but diagnostics; the labels must describe the maximum, so
they describe a signed-in user.

**Tracking: none.** No data is used to track, no ad SDK, no third-party
advertising identifier. Answer *No* to the tracking question, and no ATT
prompt is needed.

| Category | Type | Collected | Linked to user | Purpose |
|---|---|---|---|---|
| Health & Fitness | **Fitness** | Yes | Yes | App Functionality |
| Health & Fitness | **Health** | **No** | — | Never leaves the device |
| Contact Info | Email address | Yes | Yes | App Functionality |
| Contact Info | Name | Yes | Yes | App Functionality |
| Identifiers | User ID | Yes | Yes | App Functionality |
| User Content | Photos or Videos (avatar, feedback screenshot) | Yes | Yes | App Functionality |
| User Content | Other User Content (feedback text) | Yes | Yes | App Functionality |
| Usage Data | Product Interaction | Yes | No | Analytics |
| Diagnostics | Crash Data, Performance Data | Yes | No | App Functionality |

Notes for whoever fills the form:

- **Fitness is collected; Health is not.** Workouts sync to the account, so
  they are fitness data that leaves the device. The six metrics read from
  HealthKit do not, and neither does anything derived from them — the guard
  table in `docs/2026-09-05.health-data.md` is the evidence. Do not tick
  *Health* to be safe: an inaccurate label is its own problem, and 5.1.3
  compliance is easier to argue when the label matches the code.
- **Usage Data** is Firebase Analytics, and only the navigator observer —
  screen views, no custom events, no health parameter, ever.
- **Diagnostics** is Sentry: exception type and stack, `tracesSampleRate` 1.0.
  No `setUser` call in `lib/core/env/sentry.dart`, so it is not linked.
- **Photos**: the avatar goes to the account; the in-app feedback screenshot
  goes with the bug report. Health values are redacted out of that capture
  (`RedactedInCapture`), which is worth a sentence in review notes.

## Play Data safety

Same substance, Google's vocabulary. Everything below is **not shared** with
third parties, **encrypted in transit**, and **deletable** — the app deletes
an account from Settings and the server honours it within
`ACCOUNT_DELETION_DEADLINE` (30 days in prod); the deletion URL is
`https://heart-of.me/privacy`.

| Data type | Collected | Optional? | Purpose |
|---|---|---|---|
| Health and fitness → **Fitness info** | Yes | Optional (account only) | App functionality |
| Health and fitness → **Health info** | **No** | — | Device-only |
| Personal info → Email address, Name, User IDs | Yes | Optional (account only) | App functionality, Account management |
| Photos and videos → Photos | Yes | Optional | App functionality, Customer support |
| App activity → App interactions | Yes | No | Analytics |
| App info and performance → Crash logs, Diagnostics | Yes | No | App functionality |

**Health Connect declaration** (App content → Health apps) is a separate
form and the slow one. It must list, and justify one by one, exactly the
permissions in the manifest: the six reads
(`READ_RESTING_HEART_RATE`, `READ_HEART_RATE_VARIABILITY`, `READ_SLEEP`,
`READ_STEPS`, `READ_ACTIVE_CALORIES_BURNED`, `READ_WEIGHT`),
`READ_HEALTH_DATA_HISTORY`, and `WRITE_EXERCISE`. Two things to plan for:

- Google asks for a **video** showing the permission flow and each granted
  permission in use. Budget a recording session; the declaration is reviewed
  in weeks, not days.
- Reading and writing are declared separately, and the answers must match
  `site/privacy.html` word for word in substance.

## Category, age rating, review notes

| | App Store | Play |
|---|---|---|
| Category | Health & Fitness (primary), Sports (secondary) | Health & Fitness |
| Age rating | 4+ | Everyone (IARC) |
| Content | No objectionable content of any kind |

**The Apple questionnaire is answered**, and returns `FOUR_PLUS`. It lives on
the **App Information** page, not the version page, which is why it is easy to
miss; over the API it is
`/v1/appInfos/{id}/ageRatingDeclaration` (the declaration's id is the
appInfo's). Apple's revised questionnaire is in force — `ageAssurance`,
`healthOrWellnessTopics`, `messagingAndChat`, `socialMedia`, `lootBox` and
`ageRatingOverrideV2` are new fields, and `medicalOrTreatmentInformation` is
an enum (`NONE`), not a boolean.

Every content category is `NONE` and every boolean false. The three answers
worth knowing the reasoning for, because the field names are wider than
Apple's definitions:

- **`healthOrWellnessTopics` — no.** Apple defines it as *content that
  provides self-care or lifestyle recommendations*. Heart recommends nothing:
  it records what you did and shows your own readings back. Goals are the
  user's own targets and records are descriptive.
- **`medicalOrTreatmentInformation` — no**, for the same reason. This is the
  field that quietly pushes fitness apps to 12+ when it is ticked out of
  caution.
- **`userGeneratedContent` — no.** Apple means content visible to *other*
  users, and guideline 1.2 requires any app declaring it to ship filtering,
  reporting and blocking. Heart has no feed, no sharing and no comments;
  avatars and feedback screenshots are user-supplied but never published.
  Declaring it invites a reviewer to ask for a report-and-block flow that
  does not exist.

All three flip the day coaching or sharing ships — that is the trigger to
reopen this section.

**App Review notes (Apple)**, worth writing out:

> The app is fully usable without an account — reviewers can log a workout,
> build a template and read every chart straight from a fresh install. A
> demo account is provided for the sync and backup features only.
>
> HealthKit: Heart reads resting heart rate, HRV, sleep, steps, active energy
> and body mass to display them beside the user's training, and writes one
> workout per finished session (start, end, activity type). Health data is
> stored on the device only and is never transmitted — it is excluded from
> the data export, from analytics, and from crash reports.

Sign in with Apple is implemented and offered wherever Google sign-in is, so
guideline 4.8 is satisfied.

## Export compliance

Nothing to answer. `ITSAppUsesNonExemptEncryption` is `<false/>` in
`ios/Runner/Info.plist`, so every build carries the answer and App Store
Connect never shows the prompt — every uploaded build reads back
`usesNonExemptEncryption=False`. If that key is ever removed, the question
returns and blocks each build individually.

## Screenshots

### Sizes

| Store | Required | Notes |
|---|---|---|
| App Store | iPhone 6.9" (1320×2868 or 1290×2796) | Apple scales the smaller iPhone sizes from it |
| App Store | iPad 13" (2064×2752 or 2048×2732) | Required because the app ships iPad support |
| Play | Phone, 2–8 shots, 9:16, 1080×1920 or larger | |
| Play | 7" and 10" tablet | Needed to avoid the "not optimised for tablets" flag |
| Play | Feature graphic 1024×500 | Required, and easy to forget |
| Play | Icon 512×512 PNG | |

Apple has been trimming the required set — confirm the current list in the
console before spending a capture session on sizes nobody asks for.

### Shot list

Six phone shots, in this order, each with a caption above the frame:

1. **Workout in progress**, rest timer running — *Every set, and the rest
   between them*
2. **Exercise library** with the body atlas — *Find the movement, or add
   your own*
3. **Profile dashboard**, charts and goals — *Watch the numbers add up*
4. **Records** after a finished session — *Every personal record, at every
   rep count*
5. **Health section** — *Your health data, beside your training and nowhere
   else*
6. **Settings: export and erase** — *Take it with you. Or erase it.*

Tablet shots are not the phone six re-rendered: use the two-pane History and
Exercises, and the navigation rail, because that is the thing being claimed.
Three is enough — two-pane History, two-pane Exercises, dashboard.

`assets/screenshots/` holds five older phone captures (timer, charts,
congrats, exercises, workout) at README resolution; they predate the current
design and are not store-sized. Capture fresh ones via the `drive-the-app`
skill, on the 6.9" simulator and the iPad Pro 13".

### People in the frames

The profile shots show an avatar. Options, in the order they cost least:

- a monogram avatar (the app's own default) — no rights question at all;
- Muffin, per the house rule that fixtures use the cat;
- a licensed photo (a real gym photo you own, or a stock licence that
  permits app-store use);
- an AI-generated portrait.

I cannot generate images here — there is no image tool in this session, and
the design tooling I do have draws layouts, not photographs. If you want AI
portraits, they have to come from an image model outside this repo, and two
rules apply when they land: they must not be presented as real users or
testimonials, and the store screenshots must still show the actual app.

## Localisation

The app ships English, Spanish, French and Russian. Both consoles take a
listing per locale, under the same limits, and a localised listing measurably
outperforms an English one in those markets. The strings above are the
English source; run them through the same review the app's copy gets before
translating, because a listing is copy like any other.
