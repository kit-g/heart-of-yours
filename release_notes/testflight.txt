Bug fixes and performance improvements.

What to test
• Open the app to Profile with health data on the device, and leave it there a moment. The health cards and the code that fills them were both reworked; this is where the last build could show an error box instead of a card.
• Finish a workout with notifications turned OFF for Heart, then again with them ON. The first path used to fail silently in the background; the second has to still deliver the rest timer.
• Sign in with Google and with Apple, on both the sign-in and the sign-up screens. All four buttons now share one control, so it is the rendering and the tap that want checking, not just one of them.
